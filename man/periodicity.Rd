% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/periodicity.R
\name{period_detector}
\alias{period_detector}
\alias{top_periods}
\title{Online periodicity detection}
\usage{
period_detector(lags = NULL, alpha = 0.01, min_observations = 50L)

top_periods(scores, threshold = 0.3, max_periods = 3L)
}
\arguments{
\item{lags}{integer candidate lags; a calendar-minded default if \code{NULL}.}

\item{alpha}{EMA rate of the running autocorrelation estimates.}

\item{min_observations}{observations before any score is emitted.}

\item{scores}{the \code{scores} list from a \code{period_detector} call.}

\item{threshold}{minimum absolute autocorrelation to count as a period.}

\item{max_periods}{number of leading scores considered.}
}
\value{
\code{period_detector} returns a function \code{f(y, state)} returning
\code{list(scores, state)}, where \code{scores} is a list of \code{c(lag, acf)} pairs
sorted by absolute autocorrelation. \code{top_periods} returns an integer
vector of periods.
}
\description{
\code{period_detector} maintains exponentially weighted autocorrelation
estimates at candidate lags, O(n_lags) per observation, returning ranked
\verb{(lag, acf)} scores; \code{top_periods} extracts the significant ones. Port of
\code{skaters/periodicity.py}.
}
\examples{
f <- period_detector(lags = c(2L, 3L), min_observations = 10L)
st <- NULL
for (y in rep(c(1, -1), 20)) {
  r <- f(y, st)
  st <- r$state
}
top_periods(r$scores)
}
