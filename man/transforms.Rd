% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{difference}
\alias{difference}
\alias{ema_transform}
\alias{standardize}
\alias{ou_transform}
\alias{theta}
\alias{drift}
\alias{holt_linear}
\alias{garch}
\alias{seasonal_difference}
\alias{seasonal_anchor}
\alias{power_transform}
\alias{yeo_johnson}
\alias{fractional_difference}
\alias{ar_transform}
\alias{grouped_ar}
\title{Invertible online transforms}
\usage{
difference()

ema_transform(alpha = 0.05)

standardize(alpha = 0.05, eps = 1e-08)

ou_transform(kappa = 0.1, alpha = 0.02)

theta(alpha = 0.1)

drift(alpha = 0.002, shrinkage = 0.001)

holt_linear(alpha = 0.1, beta = 0.05)

garch(omega = 0.01, alpha = 0.1, beta = 0.85, mean_alpha = 0.05, eps = 1e-08)

seasonal_difference(period = 12L)

seasonal_anchor(period, alpha = 0.2, weight = 0.5)

power_transform(p = 0.5)

yeo_johnson(lmbda = 0)

fractional_difference(d = 0.4, window = 50L)

ar_transform(order = 2L, lam = 0.99, ridge = 1, decay = 0)

grouped_ar(max_lag = 16L, lam = 0.99, ridge = 1)
}
\arguments{
\item{alpha}{smoothing rate of the running level, variance, or mean
(for \code{garch}, the ARCH coefficient).}

\item{eps}{variance floor guarding the division.}

\item{kappa}{mean-reversion rate of the Ornstein-Uhlenbeck pull.}

\item{shrinkage}{pull of the drift estimate toward zero.}

\item{beta}{trend smoothing rate (for \code{garch}, the GARCH coefficient).}

\item{omega}{constant term of the GARCH variance recursion.}

\item{mean_alpha}{EMA rate of the running mean whose deviations the
variance tracks.}

\item{period}{season length in observations.}

\item{weight}{blend weight of the phase-indexed EMA against the
seasonal-naive value.}

\item{p}{exponent of the signed power map.}

\item{lmbda}{Yeo-Johnson lambda.}

\item{d}{fractional differencing order.}

\item{window}{truncation window of the fractional filter.}

\item{order}{autoregressive order.}

\item{lam}{forgetting factor of the recursive least-squares fit.}

\item{ridge}{ridge penalty of the fit.}

\item{decay}{per-step decay of the fitted coefficients toward zero.}

\item{max_lag}{largest lag available to the grouped fit.}
}
\value{
a transform: \code{list(forward, inverse_k)}, where
\code{forward(y, tstate)} returns the transformed value and state, and
\code{inverse_k(dists, tstate)} maps \code{k} predictive distributions back to
the original coordinates. Consumed by \code{\link[=conjugate]{conjugate()}}.
}
\description{
The transform grammar: each factory returns a forward/inverse pair that
\code{\link[=conjugate]{conjugate()}} wraps around an inner skater, so the inner skater models
the transformed residual and predictions are mapped back. Port of
\code{skaters/transform.py}, parity-checked at 1e-6. The reference spells the
autoregressive transform \code{ar}; it is exported here as \code{ar_transform}
(matching \code{ema_transform} and \code{ou_transform}) so attaching the package
does not mask \code{stats::ar}. Note \code{garch} here is the variance-tracking
transform, not a fitter.
}
\details{
\code{seasonal_difference} subtracts the value one period ago;
\code{seasonal_anchor} subtracts a hedge between that seasonal-naive value and
a phase-indexed EMA, which adapts as fast as the naive without inheriting
its single-draw noise (\code{weight = 0} recovers \code{seasonal_difference}).
}
\examples{
f <- conjugate(leaf(k = 1), ema_transform(alpha = 0.1), k = 1)
st <- NULL
for (y in c(5, 5.2, 4.9, 5.1)) {
  r <- f(y, st)
  st <- r$state
}
dist_mean(r$dists[[1]])
}
