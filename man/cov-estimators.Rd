% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cov.R
\name{running_cov}
\alias{running_cov}
\alias{ema_cov}
\alias{ledoit_wolf_cov}
\title{Online covariance estimation}
\usage{
running_cov(y, state = NULL)

ema_cov(y, state = NULL, alpha = 0.05)

ledoit_wolf_cov(y, state = NULL, alpha = 0.05, shrinkage = 0.5)
}
\arguments{
\item{y}{numeric vector, one observation of the multivariate series.}

\item{state}{the list returned by the previous call, or \code{NULL} to start.}

\item{alpha}{EMA rate: weight given to the newest observation.}

\item{shrinkage}{weight of the identity target in the Ledoit-Wolf
shrinkage of the EMA correlation matrix.}
}
\value{
\code{list(mean, cov, state)}: the running mean vector, the covariance
estimate as a flat row-major vector, and the state to pass back in.
}
\description{
Streaming estimators processing one observation vector at a time:
\code{running_cov} (Welford), \code{ema_cov} (exponentially weighted), and
\code{ledoit_wolf_cov} (EMA correlations shrunk toward identity).
Port of \verb{skaters/cov/}.
}
\examples{
st <- NULL
for (i in 1:10) {
  r <- running_cov(c(sin(i), cos(i)), st)
  st <- r$state
}
matrix(r$cov, 2, 2)
}
