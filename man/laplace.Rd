% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{build_candidates}
\alias{build_candidates}
\alias{laplace}
\title{laplace: the general forecaster}
\usage{
build_candidates(k, leaf_fn = leaf)

laplace(
  k = 1L,
  objective = c("crps", "likelihood"),
  sticky = TRUE,
  leaf = NULL,
  scales = NULL,
  scale_alpha = 0.03,
  tails = c("gpd", "gaussian")
)
}
\arguments{
\item{k}{forecast horizon in steps.}

\item{leaf_fn}{leaf factory used for every candidate, e.g. \code{\link[=leaf]{leaf()}}.}

\item{objective}{trunk weighting objective: \code{"crps"} or \code{"likelihood"}.}

\item{sticky}{whether to wrap in the lattice projection for repeat-heavy
series (see \code{\link[=sticky]{sticky()}}).}

\item{leaf}{leaf factory for the candidate population; the default
follows \code{objective}.}

\item{scales}{scale basis passed to the leaf factories; \code{NULL} for the
objective's default.}

\item{scale_alpha}{adaptation rate of the leaf scale weights.}

\item{tails}{\code{"gpd"} to splice generalized-Pareto tails onto the
predictive (see \code{\link[=gpdtails]{gpdtails()}}), \code{"gaussian"} for none.}
}
\value{
\code{build_candidates} returns \code{list(candidates, depths)}: the
candidate skaters and their tree depths, in the reference order.

a skater: a function \code{f(y, state)} returning \code{list(dists, state)}
with one predictive mixture per horizon; \code{state$pit} and \code{state$z}
carry the parade's calibration diagnostics (see \code{\link[=parade]{parade()}}).
}
\description{
The deployed default: a likelihood-weighted trunk over the candidate
population from \code{\link[=build_candidates]{build_candidates()}}, a CRPS terminal leaf, the lattice
projection, multi-scale at \code{k > 1}, the GPD tail splice, and the parade
wrapper. Call as \code{f <- laplace(k)}, then feed each observation with
\code{f(y, state)}; the returned \code{dists} holds one mixture per horizon.
Port of \code{skaters/api.py}.
}
\examples{
f <- laplace(k = 1)
st <- NULL
for (y in c(0.4, -0.2, 0.1, 0.6, -0.3, 0.2)) {
  r <- f(y, st)
  st <- r$state
}
dist_mean(r$dists[[1]])
dist_quantile(r$dists[[1]], 0.95)
}
