% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parade.R
\name{parade}
\alias{parade}
\title{Prediction parade: PIT/z calibration state}
\usage{
parade(base, k)
}
\arguments{
\item{base}{the skater to wrap.}

\item{k}{forecast horizon in steps; must match \code{base}'s.}
}
\value{
a skater: a function \code{f(y, state)} returning \code{list(dists, state)}
with \code{state$pit} and \code{state$z} holding the per-horizon diagnostics
(\code{NA} until a prediction has matured).
}
\description{
Pass-through wrapper that scores each arriving observation against the
predictive issued for it, exposing per-horizon PIT values and normal
z-scores in \code{state$pit} and \code{state$z}. Also gates pathological inputs
before the tree consumes them. Port of \code{skaters/parade.py}.
}
