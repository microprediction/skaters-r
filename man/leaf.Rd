% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leaf.R
\name{leaf}
\alias{leaf}
\alias{scale_mixture_leaf}
\alias{crps_leaf}
\alias{garch_leaf}
\title{Leaf skaters: residual noise models}
\usage{
leaf(k = 1L)

scale_mixture_leaf(
  k = 1L,
  gamma = 0.02,
  scale_alpha = 0.01,
  scales = .SCALE_BASIS
)

crps_leaf(k = 1L, eta = 1, scale_alpha = 0.01, scales = .FINE)

garch_leaf(
  k = 1L,
  gamma = 0.02,
  refit_every = 40L,
  min_obs = 80L,
  window = 400L,
  scales = .SCALE_BASIS
)
}
\arguments{
\item{k}{forecast horizon in steps.}

\item{gamma}{per-step decay of the scale weights.}

\item{scale_alpha}{adaptation rate of the base standard deviation.}

\item{scales}{numeric vector of standard-deviation multipliers forming
the scale basis.}

\item{eta}{learning rate of the CRPS weight update.}

\item{refit_every}{observations between GARCH refits.}

\item{min_obs}{observations required before the first refit.}

\item{window}{observations retained for refitting.}
}
\value{
a skater: a function \code{f(y, state)} returning \code{list(dists, state)}
with one predictive mixture per horizon (see \code{\link[=dist_new]{dist_new()}}).
}
\description{
The terminal noise models of the transform tree. \code{leaf} is the
running-moments Gaussian; \code{scale_mixture_leaf} learns a mixture over a
scale basis by likelihood; \code{crps_leaf} learns it by CRPS; \code{garch_leaf}
refits a GARCH(1,1) window. Each returns a skater \verb{function(y, state)}
yielding \code{list(dists, state)}. Port of \code{skaters/leaf.py}.
}
