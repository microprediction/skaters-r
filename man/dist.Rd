% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist.R
\name{dist_new}
\alias{dist_new}
\alias{dist_gaussian}
\alias{dist_combine}
\alias{dist_mean}
\alias{dist_var}
\alias{dist_std}
\alias{dist_logpdf}
\alias{dist_cdf}
\alias{dist_crps}
\alias{dist_quantile}
\alias{dist_shift}
\alias{dist_scale}
\alias{dist_affine}
\alias{dist_prune}
\title{Gaussian mixture distribution: the distributional prediction type}
\usage{
dist_new(w, m, s)

dist_gaussian(mean = 0, std = 1)

dist_combine(dists, weights = NULL)

dist_mean(d)

dist_var(d)

dist_std(d)

dist_logpdf(d, x)

dist_cdf(d, x)

dist_crps(d, x)

dist_quantile(d, p, tol = 1e-09, max_iter = 100L)

dist_shift(d, delta)

dist_scale(d, factor)

dist_affine(d, a, b)

dist_prune(d, max_components = 20L)
}
\arguments{
\item{w}{numeric vector of mixture weights (normalized internally).}

\item{m}{numeric vector of component means.}

\item{s}{numeric vector of component standard deviations.}

\item{mean}{mean of the single Gaussian component.}

\item{std}{standard deviation of the single Gaussian component.}

\item{dists}{list of dists to combine into one mixture.}

\item{weights}{weights over \code{dists}; equal by default.}

\item{d}{a dist.}

\item{x}{point at which to evaluate the density, cdf, or CRPS.}

\item{p}{probability in (0, 1).}

\item{tol}{bisection tolerance for the quantile search.}

\item{max_iter}{bisection iteration cap.}

\item{delta}{shift added to every component mean.}

\item{factor}{scale applied to means and (in absolute value) to sds.}

\item{a}{multiplier of the affine map \code{a * x + b}; must be nonzero.}

\item{b}{offset of the affine map.}

\item{max_components}{component budget; closest pairs are moment-matched
and merged until the mixture fits.}
}
\value{
Constructors and affine maps return a dist: \code{list(w, m, s)}.
\code{dist_mean}, \code{dist_var}, \code{dist_std}, \code{dist_logpdf}, \code{dist_cdf},
\code{dist_crps}, and \code{dist_quantile} return a scalar.
}
\description{
Constructors and probes for the Gaussian mixture that every skater emits:
\code{dist_new(w, m, s)} builds one, \code{dist_gaussian} a single component,
\code{dist_combine} a weighted mixture of mixtures. Probes: \code{dist_mean},
\code{dist_var}, \code{dist_std}, \code{dist_logpdf}, \code{dist_cdf}, \code{dist_crps},
\code{dist_quantile}. Affine maps: \code{dist_shift}, \code{dist_scale}, \code{dist_affine}.
\code{dist_prune} reduces the component count by ulp-tolerant closest-pair
merging. Port of the Python reference \code{skaters/dist.py}, parity-checked
at 1e-6.
}
\details{
The probes also accept the tail-spliced distributions produced by
\code{\link[=gpdtails]{gpdtails()}} and dispatch to the \verb{spliced_*} functions for them.
}
\examples{
d <- dist_new(c(0.5, 0.5), c(0, 1), c(1, 2))
dist_mean(d)
dist_quantile(d, 0.9)
dist_crps(d, 0.3)
dist_mean(dist_shift(d, 10))
}
