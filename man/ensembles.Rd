% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian.R, R/ensemble.R, R/terminal.R
\name{bayesian_ensemble}
\alias{bayesian_ensemble}
\alias{precision_weighted_ensemble}
\alias{terminal_leaf_ensemble}
\title{Skater ensembles}
\usage{
bayesian_ensemble(
  skaters,
  k = 1L,
  learning_rate = 0.5,
  complexity_penalty = 0,
  depths = NULL,
  prior_log_weights = NULL,
  max_components = 20L
)

precision_weighted_ensemble(skaters, k = 1L, floor = 1e-06)

terminal_leaf_ensemble(
  skaters,
  leaf_fn = crps_leaf,
  k = 1L,
  learning_rate = 0.5,
  complexity_penalty = 0,
  depths = NULL,
  prior_log_weights = NULL,
  max_components = 20L,
  forget = 1
)
}
\arguments{
\item{skaters}{list of member skaters, each \verb{function(y, state)}.}

\item{k}{forecast horizon in steps.}

\item{learning_rate}{multiplier on per-step log-likelihood in the weight
update.}

\item{complexity_penalty}{per-step penalty proportional to member depth.}

\item{depths}{numeric vector of member depths for the complexity penalty;
zero if \code{NULL}.}

\item{prior_log_weights}{starting log-weights; uniform if \code{NULL}.}

\item{max_components}{component budget for the combined mixture, enforced
by \code{\link[=dist_prune]{dist_prune()}}.}

\item{floor}{lower bound on any member's weight.}

\item{leaf_fn}{leaf factory fitted to the ensemble's residual stream,
e.g. \code{\link[=crps_leaf]{crps_leaf()}}.}

\item{forget}{per-step decay applied to accumulated member weights.}
}
\value{
a skater: a function \code{f(y, state)} returning \code{list(dists, state)}
whose predictive is the weighted mixture of the members'.
}
\description{
\code{precision_weighted_ensemble} weights members by inverse mean-squared
error; \code{bayesian_ensemble} by exponentiated log-likelihood with a
complexity penalty; \code{terminal_leaf_ensemble} adds a terminal leaf fitted
to the ensemble's own residuals (the trunk of \code{\link[=laplace]{laplace()}}). Ports of
\code{skaters/ensemble.py}, \code{bayesian.py}, and \code{terminal.py}.
}
