% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{spec_build}
\alias{spec_build}
\alias{spec_name}
\alias{leaf_spec}
\alias{ema_spec}
\alias{ensemble_spec}
\alias{conjugate_spec}
\alias{diff_spec}
\alias{frac_spec}
\alias{std_spec}
\alias{ema_t_spec}
\title{Symbolic specification of skater pipelines}
\usage{
spec_build(spec)

spec_name(spec)

leaf_spec(k = 1L)

ema_spec(alpha = 0.05, k = 1L)

ensemble_spec(..., k = 1L)

conjugate_spec(skater_spec, transform_spec)

diff_spec()

frac_spec(d = 0.4, window = 50L)

std_spec(alpha = 0.05)

ema_t_spec(alpha = 0.05)
}
\arguments{
\item{spec}{a spec list, from the \verb{*_spec} constructors.}

\item{k}{forecast horizon in steps.}

\item{alpha}{EMA rate.}

\item{...}{member specs of the ensemble.}

\item{skater_spec}{spec of the inner skater.}

\item{transform_spec}{spec of the transform wrapped around it.}

\item{d}{fractional differencing order.}

\item{window}{truncation window of the fractional filter.}
}
\value{
\code{spec_build} returns the materialized skater; \code{spec_name} a
string; the constructors return spec lists.
}
\description{
A spec is a plain list describing how to build a skater; it can be
serialized, compared, and materialized with \code{spec_build}. \code{spec_name}
derives the canonical name (transform chains read left to right, e.g.
\code{"diff|ema_t(0.1)|leaf"}). The \verb{*_spec} functions are constructors for
the grammar. Port of \code{skaters/spec.py}.
}
\examples{
sp <- conjugate_spec(leaf_spec(k = 1), diff_spec())
spec_name(sp)
f <- spec_build(sp)
r <- f(1.5, NULL)
}
