% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conjugate.R
\name{conjugate}
\alias{conjugate}
\title{Conjugate a skater by an invertible transform}
\usage{
conjugate(skater, transform, k = 1L)
}
\arguments{
\item{skater}{a skater: a function \code{f(y, state)} returning
\code{list(dists, state)}, such as \code{\link[=leaf]{leaf()}}.}

\item{transform}{a transform pair \code{list(forward, inverse_k)}, such as
\code{\link[=difference]{difference()}}; see the transforms page.}

\item{k}{forecast horizon in steps; must match the inner skater's.}
}
\value{
a skater \verb{function(y, state)} returning \code{list(dists, state)} in
the original coordinates.
}
\description{
Runs the transform forward on each observation, lets the inner skater
forecast the transformed series, and pushes the predictive distributions
back through the inverse. The composition operator of the whole package.
Port of \code{skaters/conjugate.py}.
}
\examples{
f <- conjugate(leaf(k = 1), difference(), k = 1)
st <- NULL
for (y in c(10, 10.5, 11.2, 10.9)) {
  r <- f(y, st)
  st <- r$state
}
dist_mean(r$dists[[1]])
}
