% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ema.R
\name{ema}
\alias{ema}
\title{EMA skater}
\usage{
ema(alpha = 0.05, k = 1L)
}
\arguments{
\item{alpha}{EMA rate: weight given to the newest observation.}

\item{k}{forecast horizon in steps.}
}
\value{
a skater: a function \code{f(y, state)} returning \code{list(dists, state)}
with one predictive mixture per horizon (see \code{\link[=dist_new]{dist_new()}}).
}
\description{
Exponential moving average point forecast with running-variance bands, as
a skater \verb{function(y, state)}. Port of \code{skaters/ema.py}.
}
\examples{
f <- ema(alpha = 0.1, k = 2)
st <- NULL
for (y in c(1, 1.2, 0.9, 1.1, 1.0)) {
  r <- f(y, st)
  st <- r$state
}
vapply(r$dists, dist_mean, numeric(1))
}
