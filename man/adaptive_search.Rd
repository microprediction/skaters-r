% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{adaptive_search}
\alias{adaptive_search}
\title{Adaptive search over the transform tree}
\usage{
adaptive_search(
  k = 1L,
  learning_rate = 0.5,
  complexity_penalty = 0.02,
  max_pool = 30L,
  expand_interval = 100L,
  expand_top_n = 3L,
  max_depth = 3L,
  replay_buffer = 500L,
  prune_threshold = -50,
  max_components = 20L,
  cost_budget = Inf
)
}
\arguments{
\item{k}{forecast horizon in steps.}

\item{learning_rate}{multiplier on per-step log-likelihood in the weight
update.}

\item{complexity_penalty}{per-step penalty proportional to pipeline depth.}

\item{max_pool}{candidate pool ceiling; the worst are pruned past it.}

\item{expand_interval}{observations between expansion rounds.}

\item{expand_top_n}{candidates expanded per round.}

\item{max_depth}{maximum transform-chain depth.}

\item{replay_buffer}{recent observations replayed into newborn candidates.}

\item{prune_threshold}{log-weight (relative to the best) below which a
candidate is dropped.}

\item{max_components}{component budget for the combined mixture.}

\item{cost_budget}{optional cap on total per-step candidate cost.}
}
\value{
a skater: a function \code{f(y, state)} returning \code{list(dists, state)}.
}
\description{
Beam search over the transform grammar: candidates are scored by
cumulative clamped log-likelihood, top performers are expanded with new
transforms (children replay recent history so they join warm), and losers
are pruned. Named \code{adaptive_search} because \code{search} would mask
\code{base::search}. Port of \code{skaters/search.py}.
}
